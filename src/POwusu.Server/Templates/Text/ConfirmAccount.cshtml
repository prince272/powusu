@using POwusu.Server.Entities.Identity
@using POwusu.Server.Extensions.Validation
@model (User User, string Code)

Hi @Model.User.FullName,

Use code: @Model.Code to confirm your phone number.

Ignore if it was not requested by you.

Regards,
Prince Yaw Owusu
