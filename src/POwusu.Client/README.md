# Prince Owusu portfolio

This is a static Next.js portfolio built with React 19, Tailwind CSS v4, and HeroUI v3.

## Run locally

```bash
npm install
npm run dev
```

The public profile is intentionally serverless: content is defined in `config/site.ts`, and downloadable documents live in `public/documents/`.
